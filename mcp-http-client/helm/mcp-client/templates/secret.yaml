apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mcp-client.fullname" . }}-stub
  labels:
    {{- include "mcp-client.labels" . | nindent 4 }}
type: Opaque
stringData:
  placeholder: "reserved-for-client-tokens-if-needed"
