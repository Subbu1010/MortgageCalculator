apiVersion: v1
kind: Service
metadata:
  name: mortgage-mcp-server
  labels:
    app: mortgage-mcp-server
spec:
  selector:
    app: mortgage-mcp-server
  ports:
    - name: http
      port: 8000
      targetPort: 8000
